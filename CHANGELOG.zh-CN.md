# 更新日志

[English](./CHANGELOG.md) | 简体中文

本项目遵循 [语义化版本（Semantic Versioning）](https://semver.org/lang/zh-CN/)，
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [Unreleased]

### Added

- 软件条目新增 `website` 官网地址字段：通过整卡覆盖链接（stretched link），点击卡片主体（下载按钮以外区域）在新标签页打开官网；该链接支持 Tab 聚焦与 Enter 键操作。
- 软件条目新增可选 `downloadText` 字段，可自定义下载按钮文字（默认为「下载」）。
- `logo` 字段支持填写 Iconify 图标名（如 `ant-design:wechat-filled`），自动从 Iconify 在线 API 获取图标；图片地址写法保持不变。
- 新增可选 `logoColor` 字段，用于设置 Iconify 图标的颜色（如 `#07c160`）。
- 新增可选 `site.repo` 字段：配置开源仓库地址后，顶栏主题切换按钮右侧显示 GitHub 图标链接（新标签页打开）；主题切换按钮与 GitHub 图标悬停时显示小手光标。
- 新增可选 `site.theme` 字段：配置默认主题模式（`light` / `dark` / `system`），仅在用户未曾手动切换主题时生效，用户的选择仍优先并持久化到 localStorage。
- 软件条目新增 `category` 分类字段，并在网格上方新增分类导航栏：固定首项「全部」，其后为从数据中自动汇总的所有不同分类（按首次出现顺序排列）。选择分类即可过滤网格，当前分类与搜索关键词按「与」逻辑叠加；窄屏下导航栏可横向滑动。
- 所有配置合并为项目根目录的单一 `config.json`（`site` + `software` 两个键），取代原先的 `src/config/site.ts` 与 `src/config/software.ts`。
- 新增可选远程数据源：在 `.env` 中配置 `VITE_API_URL`（见 `.env.example`），即可从返回结构与 `config.json` 相同的远程接口动态获取页面数据。页面先以本地数据渲染，请求成功后自动替换；未配置该变量、请求失败或响应结构不符时，自动回退到本地 `config.json`。
- 新增 GitHub Pages 自动部署工作流（`.github/workflows/deploy.yml`）：每次推送到 `main` 自动构建并部署。部署路径三级优先级推断——仓库变量 `VITE_BASE` 手动覆盖 > 检测到自定义域名则使用根路径 > 仓库名规则（`<用户名>.github.io` 为根路径，项目站点为 `/<仓库名>/`）；构建时同时从仓库变量（Actions Variables）读取 `VITE_API_URL` 注入，未设置则使用本地 `config.json`。
- 新增 `config.json.example` 默认配置模板：`config.json` 随仓库正常提交、克隆后即可使用；需要将配置重置为默认时，从该模板复制覆盖即可。部署工作流仅在 config.json 意外缺失时才用模板兜底。

### Changed

- 卡片布局改为瀑布流：内联下载面板展开时只向下推开同列卡片，其他列保持不动。
- 内联下载面板改为点击按钮/面板以外区域时收起，不再随鼠标移出卡片收起。
- 卡片描述最多显示三行（原先为两行）。
- logo 容器不再带默认背景色，logo 直接呈现在卡片底色上。
- 鼠标悬停卡片时，logo 不再放大。
- 下载按钮由饱和实心蓝改为柔和的浅蓝色调风格，浅色/暗色模式均已适配。
- 瀑布流布局改由 [vue-waterfall-mini](https://github.com/Dave-12138/vue-waterfall-mini) 驱动：卡片通过 Vue 响应式样式绑定定位（替代自研瀑布流组合式函数），搜索过滤/宽度变化使用组件内置重排动画，新加入卡片播放 fade-up 加入动画。
- `website` 字段改为可选：不配置时卡片主体不可点击跳转（此前为必填）。
- 分类导航栏页面加载时默认选中「装机必备」分类。

### Fixed

- 配置中的本地图片路径（如 `/icons/xxx.svg`）现在会在渲染时拼接部署 base 路径，部署到 GitHub Pages 项目站点等子路径场景时不再 404。
- 远程配置数据到达后，当前选中的分类在新数据中不存在时，现在会自动重置为「全部」，不再出现网格为空的情况。
- 标题首字不再从 Iconify 图标的透明留白处透出。首字现在只在 `logo` 为空字符串时显示；只要配置了 logo（Iconify 名或图片地址），就不渲染首字。
- 视口缩窄后再恢复变宽时，卡片现在会正确重新分列（此前保持窄屏时的列位置）。列数只要发生变化（无论增减）都会重置列归属。

## [0.1.0] - 2026-09-23

首个开源版本。

### Added

- 配置文件驱动的软件列表：站点文案与软件数据均在 `src/config/` 中维护，组件不含硬编码数据
- 软件卡片网格：logo、标题、一句话说明与下载操作
- 单地址直下、多地址选择两种下载交互
  - `inline`：卡片内联展开地址列表（仅撑高当前卡片）
  - `popover`：浮层弹出地址列表（不改变卡片高度，支持 Esc 与遮罩关闭、空间不足自动翻转）
- 多关键词模糊搜索（标题与说明匹配，关键词之间为「与」逻辑，大小写不敏感）与空状态提示
- 亮色 / 暗色 / 跟随系统三种主题，偏好持久化到 localStorage，首屏防闪烁
- 响应式布局：移动端单列，依次在 sm / lg / xl 断点切换为两列 / 三列 / 四列
- 暗色模式对比度满足 WCAG AA，尊重系统「减少动态效果」设置
