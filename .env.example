# 远程配置接口地址（可选）
# ------------------------------------------------------------------
# 配置后，页面数据将从该接口获取；接口需返回与根目录 config.json
# 相同结构的 JSON：
#   { "site": { "name": ..., "tagline": ..., "footer": ..., "downloadPanelMode": ... },
#     "software": [ { "id": ..., "title": ..., "category": ..., ... } ] }
#
# 不配置或留空时，默认使用项目根目录的 config.json 作为数据源；
# 接口请求失败或响应结构不符时，也会自动回退到 config.json。
#
# 使用方法：复制本文件为 .env 并填写地址（.env 已被 git 忽略）。
# VITE_API_URL=https://example.com/config.json
